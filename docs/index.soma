(python) >use (markdown) >use

>md.start

(SOMA v1.1 Language Specification) >md.h1

(State-Oriented Machine Algebra) >md.p

(TL;DR) >md.h2

(SOMA is a computational model that treats programs as explicit state-transforming machines. Unlike functional languages that begin with a calculus and simulate mutation through abstractions, SOMA starts with the machine itself: mutable state, visible memory, and observable execution. There are no hidden stacks, no return semantics, no exceptions. Instead, computation flows through three structures—the Accumulator List (a stack-like value conduit\29\, the Store (a hierarchical graph of named cells\29\, and the Register (execution-local storage\29\—transformed by blocks that are values, not functions.) >md.p

(Core Philosophy:) >md.p

(- Blocks are first-class values — code is data, execution is explicit via `>`) >md.p

(- `>choose` selects, doesn't execute — selector-based branching (choose + `>^` to execute\29\) >md.p

(- `>chain` enables tail-call optimization — loops and recursion without stack growth) >md.p

(- Clean execution patterns — `>func` from paths, `>{ }` for literals, `>^` from AL) >md.p

(- Functional elegance meets imperative clarity — algebraic control flow with visible state) >md.p

(SOMA doesn't reduce expressions; it runs programs by evolving state step by step, making mutation the primary semantic domain rather than an impurity to be hidden.) >md.p

(Hello World) >md.h2

(This pushes a string onto the Accumulator List, then executes the `print` block which consumes it.) >md.p

(A more elaborate example showing blocks, execution patterns, and state:) >md.p

(This creates a block that pops a value into Register storage, concatenates it with \22\Hello \22\, and prints the result. The block is stored at Store path `greet`, then executed with \22\world\22\ on the stack.) >md.p

(Key execution patterns:) >md.p

(Implementation Status) >md.h2

(✅ Complete Reference Implementation:) >md.p

(- Lexer (`soma/lexer.py`\29\ - Full tokenization with unicode escape sequences) >md.p

(- Parser (`soma/parser.py`\29\ - AST generation with path validation) >md.p

(- VM/Runtime (`soma/vm.py`\29\ - Complete virtual machine with AL, Store, and Register) >md.p

(- Standard Library (`soma/stdlib.soma`\29\ - Derived operations built on FFI primitives) >md.p

(- Test Framework (`tests/run_soma_tests.py`\29\ - Automated test runner with EXPECT_AL/EXPECT_OUTPUT) >md.p

(- Examples (`examples/`\29\ - Working programs demonstrating SOMA concepts) >md.p

(  - `examples/sin_calculator/` - Taylor series sin(x\29\ computation with scaled integer arithmetic) >md.p

(Run SOMA:) >md.p

(Table of Contents) >md.h2

(Core Language) >md.h3

(- ) (Philosophy) (core/philosophy.md) >md.l ( - SOMA's design principles and relationship to other computational models) >md.t >md.p

(- ) (Lexical Analysis and Parsing) (core/lexer.md) >md.l ( - Tokens, AST nodes, path parsing, and validation) >md.t >md.p

(- ) (Machine Model) (core/machine-model.md) >md.l ( - The Accumulator List, Store, Registers, and Values) >md.t >md.p

(- ) (Blocks and Execution) (core/blocks-execution.md) >md.l ( - First-class blocks and the execution model) >md.t >md.p

(- ) (Control Flow) (core/control-flow.md) >md.l ( - `>choose`, `>chain`, and algebraic flow control) >md.t >md.p

(- ) (Higher-Order Patterns) (core/higher-order-patterns.md) >md.l ( - Dispatch tables, state machines, and advanced control flow) >md.t >md.p

(- ) (Built-ins) (core/builtins.md) >md.l ( - Core operations and their AL contracts) >md.t >md.p

(- ) (Standard Library) (core/stdlib.md) >md.l ( - Derived operations built from FFI primitives) >md.t >md.p

(Programming) >md.h3

(- ) (Comparisons) (programming/comparisons.md) >md.l ( - SOMA vs Forth, Haskell, Lambda Calculus, and others) >md.t >md.p

(- ) (Examples) (programming/examples.md) >md.l ( - Working SOMA programs demonstrating key concepts) >md.t >md.p

(- ) (Programming Idioms) (programming/idioms.md) >md.l ( - Idiomatic patterns, best practices, and common anti-patterns) >md.t >md.p

(- ) (Errors and Semantics) (programming/errors-semantics.md) >md.l ( - Fatal errors, abstract machine, and formal grammar) >md.t >md.p

(Extensions) >md.h3

(- ) (Extensions Overview) (extensions/extensions.md) >md.l ( - Extension system and `>use` builtin) >md.t >md.p

(- ) (Python Interface) (extensions/python-interface.md) >md.l ( - Python FFI integration) >md.t >md.p

(- ) (Load Extension) (extensions/load.md) >md.l ( - File loading capabilities) >md.t >md.p

(Debugging) >md.h3

(- ) (Debugging) (debugging/debugging.md) >md.l ( - Debug tools, philosophy, and troubleshooting techniques) >md.t >md.p

(- ) (Debug Ideas) (debugging/debug-ideas.md) >md.l ( - Proposals for future debug features) >md.t >md.p

(Markdown Extension) >md.h3

(- ) (Markdown Overview) (markdown/index.md) >md.l ( - SOMA markdown generation system) >md.t >md.p

(- ) (Markdown Skill) (markdown/SKILL.md) >md.l ( - AI assistant reference for markdown extension) >md.t >md.p

(Concepts) >md.h3

(- ) (Engineering Standards) (concepts/engineering-standards.md) >md.l ( - Verification-first development methodology) >md.t >md.p

(---) >md.p

(*Category: Informational | Version: 1.1 | Date: 20 Nov 2025*) >md.p

>md.print
